#include <stdio.h>
/**
 * main - main function of the file
 * Description: A file to print out strings stored in the array declared
 * Return: The file returns a value of zero to terminate the program
 */

int main(void)

{
	/* Declaration of strings using array */
	char mystr[] = "with proper grammar, but the outcome is a piece of art,";

	/* Printing string to stdout using the printf function */
	printf("%s\n", mystr);

	return (0);
}
